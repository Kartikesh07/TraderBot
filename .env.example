# --- Optional Environment Variables ---
# These are NOT required to run the bot (all APIs are public/unauthenticated)
# But you can override defaults here if needed.

# Render deployment port (set automatically by Render)
# PORT=10000
